%section.content
  .ui.segment
    .schedules
      %h1.ui.grey.centered.header{:style => "margin-top: 25px;"} ANNOUNCEMENTS
      %hr
    - @announcements.each do |a|
      %h3.text-blue
        = a.title
        .ui.label
          = a.announced_at
      %p
        = a.content
      %hr
